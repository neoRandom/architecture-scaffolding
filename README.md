input:
	use cases, models, services, driver adapters, driven adapters (what it imagines that the code will look like)

parsing:
	2D view of input

then input:
	interfaces (both ways)
	model contents
	DTOs

final output:
	create architecture.md
	create files
	write boilerplate code
	generate mermaid
